apiVersion: v1
kind: Service
metadata:
  name: devops-ci-lab-service
spec:
  type: NodePort
  selector:
    app: devops-ci-lab
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30007