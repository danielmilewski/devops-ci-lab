name: CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set image tag
        run: echo "IMAGE_TAG=${GITHUB_SHA}" >> $GITHUB_ENV

      - name: Log in to Docker Hub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Build Docker image
        run: docker build -t "${{ secrets.DOCKER_USERNAME }}/devops-ci-lab:${IMAGE_TAG}" .

      - name: Test container
        run: |
          docker run -d -p 8080:5000 "${{ secrets.DOCKER_USERNAME }}/devops-ci-lab:${IMAGE_TAG}"
          sleep 5
          curl -f http://localhost:8080

      - name: Push Docker image
        run: docker push "${{ secrets.DOCKER_USERNAME }}/devops-ci-lab:${IMAGE_TAG}"
